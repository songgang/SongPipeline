function mdiff = get_relative_difference(mtable) 

nb_img = size(mtable, 1);

% mavg = prod(mtable, 1) .^ (1/nb_img);

for ii = 1:nb_img;
    idx_
mavg = mean(mtable, 1);
mdiff = log(abs(mavg ./ repmat(mtable, [nb_img, 1]))+1);

